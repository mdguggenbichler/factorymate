/** Debounce interval before persisting graph changes after edits. */
export const PLANNER_GRAPH_SAVE_DEBOUNCE_MS = 800

/** Edit-lock heartbeat interval while the user holds the lock. */
export const PLANNER_LOCK_HEARTBEAT_MS = 45_000

/** Default clock speed for new process nodes (%). */
export const PLANNER_DEFAULT_CLOCK_PERCENT = 100

/** Canvas layout spacing (matches backend solver). */
export const PLANNER_LAYOUT_X_SPACING = 220
export const PLANNER_LAYOUT_Y_SPACING = 120
