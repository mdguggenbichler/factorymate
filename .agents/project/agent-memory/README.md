# Agent memory (optional)

Session notes for orchestrator sub-agents. **Never commit** this directory.

Path pattern: `active/<SESSION-ID>.md` where SESSION-ID = `<MILESTONE>-<YYYYMMDD>-<4hex>`.

Orchestrator may list filenames here but should not read contents (execution agent owns session memory).
